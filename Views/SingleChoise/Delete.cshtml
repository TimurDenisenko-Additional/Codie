@model Codie.Models.SingleChoiseModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Tasks";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
    <link rel="stylesheet" href="~/Content/style.css">

</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>SingleChoiseModel</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Statement)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Statement)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Options)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Options)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
</body>
</html>
